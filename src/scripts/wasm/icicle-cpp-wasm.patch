--- a/src/Cargo.toml
+++ b/src/Cargo.toml
@@ -14,11 +14,15 @@
 split-debuginfo = "unpacked"
 lto = false
 
+[features]
+default = ["jit"]
+jit = ["icicle-vm/jit"]
+
 [dependencies]
-icicle-vm = { git = "https://github.com/icicle-emu/icicle-emu" }
-icicle-cpu = { git = "https://github.com/icicle-emu/icicle-emu" }
-sleigh-runtime = { git = "https://github.com/icicle-emu/icicle-emu" }
-sleigh-compile = { path = "./sleigh/sleigh-compile" }
+icicle-vm = { path = "../../icicle-emu/icicle-vm", default-features = false }
+icicle-cpu = { path = "../../icicle-emu/icicle-cpu" }
+sleigh-runtime = { path = "../../icicle-emu/sleigh/sleigh-runtime" }
+sleigh-compile = { path = "../../icicle-emu/sleigh/sleigh-compile" }
 indexmap = "1.9.1"
 target-lexicon = "0.13"
 tracing-subscriber = "0.3"
@@ -29,5 +33,5 @@
 bincode = "1.3.3"
 zstd = "0.13.0"
 bytemuck = "1"
-pcode = { git = "https://github.com/icicle-emu/icicle-emu" }
+pcode = { path = "../../icicle-emu/sleigh/pcode" }
 ahash = "0.8.11"
--- a/src/vm.rs
+++ b/src/vm.rs
@@ -79,8 +79,10 @@
             return Err(format!("Unknown architecture: {}", architecture));
         }
 
-        config.enable_jit = jit;
-        config.enable_jit_mem = jit_mem;
+        // Without the `jit` feature (e.g. wasm builds) the VM only has the
+        // interpreter, so the JIT must stay off regardless of the request.
+        config.enable_jit = jit && cfg!(feature = "jit");
+        config.enable_jit_mem = jit_mem && cfg!(feature = "jit");
         config.enable_shadow_stack = shadow_stack;
         config.enable_recompilation = recompilation;
         config.track_uninitialized = track_uninitialized;
